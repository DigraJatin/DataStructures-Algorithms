[20:53, 26/8/2026] Jatin: had my mock round today:

initial question was very vague, and was kind of incorrect... so the itnerviewer corrected the question upon asking in middle of the round.
The question boiled down to: 
given an undirected graph in the below format, find whether a triangle exists in the graph or not:
std::unordered_map<std::string, std::vector<std::string>> graph = {
    {"A", {"B", "C"}},
    {"B", {"A", "C"}},
    {"C", {"A", "B"}},
};

Then on follow-up questions, i was asked how i would modify my code, if asked to find a quadrilateral vs a n-degree polynomial
[20:53, 26/8/2026] Jatin: Had my screen today. This question was asked 

Given A,B,C sorted arrays and D 

Find count of tuples where these conditions satisfy 

1. Abs(A[i] - B[j]) <= D 
2. ⁠Abs(A[i] - C[k]) <= D 
3. ⁠Abs(B[j] - C[k]) <= D
[23:32, 26/8/2026] Jatin: Longest Substring with At Most K Odd-Frequency Characters Given a string s and an integer K, find the maximum length of a substring in which at most K distinct characters have an odd frequency.